# 方寸集 · 部署上线指南

> **给谁看**：负责把网站从"本地预览"变成"任何人任何设备都能访问的公开网址"的 AI 助手，以及站长本人。
> **对应 PRD 里的**：M3 里程碑。

---

## 全局说明：两类步骤，别搞混

部署分两种性质完全不同的步骤：

| 类型 | 谁来做 | 举例 |
|---|---|---|
| **账号/授权类** | ⚠️ **必须站长本人操作**，AI 不能代劳 | 注册 GitHub/Vercel 账号、浏览器里登录授权、点击"允许访问" |
| **技术操作类** | ✅ AI 可以完全代劳 | 初始化 git 仓库、写 commit、执行 push、检查构建日志 |

AI 遇到账号/授权类步骤时，要**清楚告诉站长"现在需要你去浏览器做 XX，做完告诉我继续"**，不要假装能自己完成，也不要让站长交出密码代为操作。

---

## 第一部分：首次上线（一次性，M3 里程碑）

### 步骤 1（账号类，站长操作）：准备 GitHub 账号

- 打开 https://github.com ，用邮箱注册一个账号（如果已有账号跳过）
- 记住用户名和密码（或设置好登录方式），后面 AI 需要你在浏览器里完成登录

### 步骤 2（技术类，AI 操作）：本地初始化 git 仓库

在项目根目录（`邮票网站/`）执行：

```bash
git init
git add .
git commit -m "方寸集 v1：数据与代码初始版本"
```

`.gitignore` 已经配置好（排除 `node_modules/`、`.next/` 等），不会把无关的大文件传上去。

### 步骤 3（账号类，站长操作）：在 GitHub 创建远程仓库

- 登录 GitHub，点右上角 "+" → "New repository"
- 仓库名随意（如 `fangcunji`），**建议设为 Private**（私有仓库，图片版权更保险；私有仓库 Vercel 一样能部署）
- 不要勾选"初始化 README"（本地已经有内容了）
- 创建后 GitHub 会显示一个仓库地址，形如 `https://github.com/你的用户名/fangcunji.git`，告诉 AI 这个地址

### 步骤 4（技术类，AI 操作）：推送代码到 GitHub

```bash
git remote add origin <站长提供的仓库地址>
git branch -M main
git push -u origin main
```

（如果 push 时要求登录，提醒站长在弹出的浏览器窗口里完成 GitHub 授权）

### 步骤 5（账号类，站长操作）：注册 Vercel 并关联 GitHub

- 打开 https://vercel.com ，选择"用 GitHub 账号登录"（不用单独设密码，直接用 GitHub 授权）
- 授权 Vercel 访问你的 GitHub 仓库（可以选择"只允许访问这一个仓库"，更安全）

### 步骤 6（账号类，站长操作，AI 可在旁指导）：导入项目并部署

- Vercel 后台点 "Add New" → "Project"
- 选中刚才创建的仓库（如 `fangcunji`）
- Vercel 会自动识别这是 Next.js 项目，**默认配置直接可用，不需要改任何设置**
- 点击 "Deploy"，等待 1–2 分钟
- 完成后 Vercel 会给一个网址，形如 `fangcunji-xxx.vercel.app` —— **这就是正式上线的网址，任何设备打开都能看**

### 步骤 7（可选，账号类，站长操作）：绑定自己的域名

**这一步完全可选，不做也不影响网站正常访问**，`xxx.vercel.app` 已经是可以长期使用的正式网址。只是想要更好记的网址（如 `fangcunji.com`）时才需要。分两种情况：

#### 情况 A：还没有域名，需要先购买

- 常见域名注册商：**阿里云万网**、**腾讯云**（国内，中文界面，付款方便）；**Namecheap**、**GoDaddy**（海外，部分后缀更便宜）
- `.com` 域名一般每年几十到一百元不等，注册时直接搜索想要的名字（如 `fangcunji.com`、`fangcunji.cn`）购买
- **国内域名注意**：域名本身购买不需要备案；但如果域名要**解析到国内服务器**才需要备案，本项目用 Vercel（境外服务器），**不涉及备案**，买完域名可以直接跳到下面"添加解析"

#### 情况 B：已经有域名，直接绑定

1. 登录 Vercel，进入这个项目 → **Settings** → **Domains**
2. 输入你的域名（如 `fangcunji.com`），点 **Add**
3. Vercel 会显示需要添加的 DNS 记录，通常是以下两种之一：
   - **根域名**（`fangcunji.com`）：需要添加一条 **A 记录**，指向 Vercel 给出的 IP 地址（通常是 `76.76.21.21`，以 Vercel 页面实际显示为准）
   - **子域名**（如 `www.fangcunji.com`）：需要添加一条 **CNAME 记录**，指向 Vercel 给出的地址（通常是 `cname.vercel-dns.com`）
4. 回到域名注册商的管理后台，找到"域名解析"/"DNS 管理"页面，按 Vercel 给的类型、主机记录、值，添加对应记录
5. 保存后等待生效（通常几分钟到几小时，最长 24 小时），Vercel 页面上域名状态会从"等待中"变成"已生效"（绿色勾）
6. **HTTPS 证书 Vercel 会自动签发**，不需要额外操作

#### 常见问题

- 同时想要 `fangcunji.com` 和 `www.fangcunji.com` 都能访问：在 Vercel Domains 里把两个都加上，Vercel 会自动处理其中一个跳转到另一个（设置里可选主域名）
- 域名注册商是国内的（阿里云/腾讯云）也完全没问题，解析记录随便填到哪个服务商都一样，不影响使用境外的 Vercel

---

## 第二部分：上线之后的日常更新流程

> ⚠️ **默认不 push，必须站长同意或要求才推送**。改完数据/代码的正常收尾是"跑体检 + 写交付说明"然后**停下**，**不要因为完成了一项工作就自动 `git push`**。只有站长明确说"可以 push / 推一下 / 上线 / 同步一下"时，才执行下面第 4 步。攒着多次改动一起推是常态，不需要每次小改都推。

首次部署完成后，**每次改数据/加邮票的流程和本地开发时几乎一样，站长发话后只多两步**：

1. 按《数据录入手册.md》正常改 `data/stamps/*.json`、导入图片
2. 跑 `python scripts/check_data.py --images`，0 ERROR
3. 本地预览确认无误（`npm run dev`）
4. **（仅在站长同意/要求后）推送前先看一眼 `git status`，精确 add 再提交**（⚠️ 这一步不能省，见下方说明）：
   ```bash
   git status --short   # 先看清单，扫一眼有没有不该出现的东西
   git add <本次相关的文件>   # 优先精确 add，不要无脑 git add -A / git add .
   git commit -m "新增 20XX 年邮票数据"
   git push
   ```
5. Vercel 检测到 GitHub 仓库有更新，**自动**重新构建，1–2 分钟后线上网站自动同步最新内容，不需要登录 Vercel 手动点任何按钮

### ⚠️ 为什么"先看 git status"不能省

这个项目文件夹**可能被多个 AI/会话同时使用**。`git status` 会显示自上次提交以来**所有**改动过的文件——不管是不是你这次任务改的，只要磁盘上有变化就会出现，包括别的会话留下的东西。这不是坏事（说明 git 天然能发现所有待同步的工作，不需要谁去"记账"），但意味着**不能无脑 `git add -A` 就提交**，扫一眼清单，重点看：

- **体积异常大的文件/文件夹**——正常改动不该出现几百 MB 以上的东西（历史上出过 24GB 图库差点被提交的事故，`.gitignore` 已经堵住了，但新出现的大文件要警惕）
- **看起来不属于这个项目的路径**——比如别的工具生成的缓存文件夹（如曾经出现过的 `.workbuddy/`）
- **和当前任务完全无关、但确实是合理项目文件的改动**（如别的会话录入的年份数据）——这种正常提交没问题，git 允许一次提交包含多个来源的改动，只是提交信息里说清楚"本次包含哪些内容"即可，不用纠结"这不是我改的要不要推"

如果发现第一类或第二类问题，**先处理掉（加进 `.gitignore` 或删除），确认 `git status` 干净后再 `add`**，不要因为"图省事"跳过这步检查。

**验证方法**：push 完之后，打开 Vercel 后台的项目页面（或直接刷新线上网址），能看到最新的构建记录和更新后的内容。

**如果构建失败**：Vercel 后台会显示报错日志（通常是本地体检没跑或漏了什么必填字段导致代码报错）。AI 处理方式：先在本地用 `npm run build` 复现问题，修好后再重新 push。**不要在 Vercel 网页上直接改代码**——所有改动源头永远是本地文件 + git push，这样才能保持"数据唯一真源在本地仓库"的原则。

---

## 第三部分：图片体积与 Vercel 免费额度

Vercel 免费版对流量和构建时长有限额，但对于个人图鉴网站（几千张几百 KB 的图片）完全够用。手册里"图片长边 ≤1600px、体积 ≤400KB"的标准（见《数据录入手册.md》第 5 节）本身就是为了控制总体积，不需要额外为部署做特别优化。如果将来图片数量涨到几千套、总体积明显变大，再考虑第三方图床（PRD 里提到的 Cloudflare R2）分流，不必现在预先做。

---

## 禁区提醒

- **绝不要**把 `新中国邮票图片全集（1949年-2026年最新）/` 这个原始图库文件夹提交进 git 仓库——它约 24GB，已在 `.gitignore` 里排除。网站只需要 `public/images/stamps/` 里筛选压缩过的图片。（注意：这个图库现在就放在项目根目录下，务必保持 `.gitignore` 里对它的排除规则，否则一次 `git add .` 会灾难性地试图上传 24GB）
- 不要把任何账号密码、API Key 写进代码或提交进仓库
- 首次部署前，检查一下仓库是否设为 Private（如果站长在意图片版权曝光问题）
