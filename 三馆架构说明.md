# 纸上山河 · 三馆架构说明

> **给谁看**：接手"纸上山河"总站开发的 AI 助手（不管是哪个会话），以及站长本人回顾时用。
> **这份文档记录什么**：为什么要做"总门厅 + 三馆"这个架构、现在做到哪一步、每一步具体改了什么文件、设计上定下来的规矩，方便任何人接手继续做第 2/3/4 步，而不用重新问一遍"为什么这么设计"。

---

## 1. 为什么会有这个架构（背景）

站长在"方寸集"邮票网站之外，还攒了另外两类素材：
- **中外名画**素材（`G:\微云同步文件夹\中外名画\`，如仇英、唐寅、葛饰北斋）
- **画册**素材（`G:\微云同步文件夹\画册\` 自制工笔画册 + `G:\微云同步文件夹\画册（原稿）\` 清代及以前的彩绘原稿），主要是给古典小说（聊斋、水浒、金瓶梅等）配图辅助理解
- **自制邮票**素材（`邮票网站\邮票形式的图片\`，如 24 孝图、孔子圣迹图，仿邮票形式但不是真实发行）

2026-07-20 与站长讨论定下：这三类内容 + 已有的方寸集邮票馆，做成"**总门厅 + 三个平级馆**"，**一个 Next.js 项目、三个路由分区**（不拆成三个独立网站/仓库），因为跨馆的标签聚合是核心诉求之一（例如自制的 24 孝图未来要和真实发行的《中华孝道》邮票在同一个主题页聚合展示）。

## 2. 总体路线（四步走）

| 步骤 | 内容 | 状态 |
|---|---|---|
| ① 总门厅 + 三分区改造 | 邮票馆整体挪入路由分组，`/` 变总门厅 | ✅ 已完成（2026-07-21） |
| ② 自制邮票进邮票馆 | `data/creations.json`、主题页"自制"一栏、标签聚合 | ⏳ 未开始 |
| ③ 画册馆 | 原稿+自制两轨，书籍翻页风格 | ✅ 首册《三十三剑客图》已完成（本地待提交） |
| ④ 名画馆 | 画廊展墙风格 | ⏳ 未开始（素材最少） |

**当前进度：第①步已完成并已推送上线；第③步已完成首册《三十三剑客图》的本地实现，尚未提交或推送。** 以下先保留第①步的架构记录；画册馆的当前文件、数据、图片与交互约定见 `docs/画册馆协作交接.md`。

---

## 3. 路由结构（第①步的实现）

```
/                       ← 总门厅"纸上山河"（app/page.tsx，根 layout.tsx 提供，见下）
/stamps                 ← 邮票馆首页（原网站首页内容原样搬入）
/stamps/[id]            ← 邮票详情页
/years, /years/[year]   ← 按年份浏览
/themes, /themes/[theme]← 按主题浏览
/collection             ← 我的收藏
/gallery                ← 中外名画馆占位页（筹备中）
/albums                 ← 画册馆馆藏目录与同册精选图轮播
/albums/[slug]          ← 单册目录、来源与整理
/albums/[slug]/[number] ← 单图阅读、放大与前后翻图
```

**关键点：邮票馆所有 URL 在改造前后完全没变**（`/years`、`/stamps/纪1` 等 1500+ 路径原样保留），只是从"就是整个网站"变成"网站的一个分区"，线上旧链接不会失效。

### 文件对应关系

- `app/layout.tsx`：**根布局**，三馆共用。只放 `<html><body>` 和页脚（footer）。**不含 header/nav**——因为门厅和邮票馆的顶部导航完全不同，各自在自己的分区里定义。
- `app/(stamps)/layout.tsx`：**邮票馆专属布局**，原来根布局里的 header/nav（方寸集 logo、按年份/按主题/我的收藏、顶栏搜索框）都在这里。`(stamps)` 是 Next.js 的 [路由分组](https://nextjs.org/docs/app/building-your-application/routing/route-groups) 语法，圆括号目录名不会出现在实际 URL 里。
- `app/(stamps)/{years,themes,collection,stamps}/`：邮票馆原有页面，从 `app/{years,themes,collection,stamps}/` 原样 `git mv` 过来，**内容没有大改**，只有链接目标做了必要调整（见下）。
- `app/page.tsx`：**总门厅页面**（新建），三馆入口卡片。
- `app/gallery/page.tsx`：名画馆占位页，等待第④步。
- `app/albums/page.tsx`、`app/albums/[slug]/`：画册馆馆藏目录、单册目录与单图阅读页；具体协作说明见 `docs/画册馆协作交接.md`。

### 因为搬家必须联动修改的地方

- `components/HeaderSearch.tsx`：原来"首页不显示顶栏搜索框"的判断是 `pathname === "/"`，现在首页变成门厅、邮票馆首页是 `/stamps`，改成 `pathname === "/stamps"`。
- `app/(stamps)/stamps/[id]/page.tsx`：面包屑"首页"链接从 `/` 改成 `/stamps`。
- `app/(stamps)/layout.tsx` 的 nav 里新增一个 **"⌂ 总馆"** 链接指向 `/`，方便从邮票馆一键回门厅。

---

## 4. 门厅视觉设计（定下来的规矩）

这部分是设计决策记录，**任何人以后改门厅或者做②③④步时，参考这里保持风格统一**。

### 4.1 总站名与斋号

- **总站名：纸上山河**（2026-07-21 定，此前用过"藏馆"作为占位名，已弃用）
- 斋号印章：**观复斋**（取《道德经》"万物并作，吾以观复"），SVG 手刻白文印，`app/page.tsx` 里的 `GuanFuSeal` 组件
  - 排布：**三字两列**——右列自上而下"观""复"（先读），左列"斋"单字放大居中占满整列高度（这是传统篆刻里"一列二字一列一字"的标准排法，单字那列要放大撑满，不然会显得空）
  - 尺寣：32px 上下，**当"落款"用，不是主视觉**——历史上试过 64-80px 放在标题正中/旁边，站长反馈"抢戏"，最终定为小尺寸放在副标题"收藏·学习·创作"那一行右端
  - 颜色：印章红 `#b23a2a`（全站 `--color-seal`）

### 4.2 门厅与各馆的视觉区分（重要教训）

**门厅最初直接沿用了邮票馆的米色点阵背景+圆角卡片，站长反馈"跟方寸集太像，没有总馆的独立感"。** 最终方案：

- **门厅背景：山水青** `#dde6e0`（配 `rgba(31,59,47,0.05)` 极细点阵），呼应"纸上山河"的山水意象，与邮票馆的米黄纸色 `#f3ecdd` 完全错开色相（不是同色系深浅，是换了色相），一进邮票馆能立刻感觉到"换了个空间"
- **门厅背景铺满整屏（含页脚）**：技术实现是 `app/globals.css` 里 `body:has(.portal-root) { ... }`，当门厅根 div 带 `portal-root` 这个 class 时，连 `<body>` 的背景色都跟着换，页脚不再是独立的米色色块。`app/layout.tsx` 的 `<footer>` 背景本身已改成透明，由 `body` 统一提供颜色——**这意味着以后画册馆/名画馆如果也想要"整屏专属背景"，同样只要在各自的根容器加一个对应的 class + 在 globals.css 写一条 `body:has(.xxx-root)` 规则即可**，是可复用的模式
- 标题下方加了一条**山峦剪影分隔线**（`MountainDivider` 组件，简单的 SVG 折线），呼应站名

### 4.3 三馆入口：书签样式

最初做过两版被否掉的方案，记录一下避免以后又走回头路：
1. ~~横向卡片（圆角+阴影）~~ → 站长反馈"跟邮票馆卡片太像"
2. ~~竖排题签风（像线装书函套书签，纯文字无边框）~~ → 站长反馈"不好看"，已回滚

**最终定案：书签形**（`Bookmark` 组件，`app/page.tsx`）：
- 竖长方形（`w-60`），不是方形也不是横向
- **顶部一道彩色"天头"横条 + 一个挂穗小孔圆点**，天头颜色是各馆的代表色：方寸集印章红 `#b23a2a`、画册靛蓝 `#2c5f8a`、中外名画金 `#c9a227`——**这三个颜色后续各馆自己的详情页/主题色可以直接沿用**，相当于提前定好了每个馆的识别色
- 馆名、副标题、统计信息/简介、"进入/筹备中"链接**全部收进书签内部**，不再有卡片外面挂着零散文字的情况
- 已开放的馆（方寸集）馆名用印章红高亮，未开放的馆名用 `text-ink/75` 灰一档，一眼区分"能点"和"筹备中"

### 4.4 门厅卡片顺序

方寸集 → 画册 → 中外名画（2026-07-21 站长定，不是按开发顺序，是站长的展示偏好）。

---

## 5. 本地启动方式

- `start-fangcunji.bat`（项目根目录）：双击后**直接打开邮票馆** `http://localhost:3011/stamps`（不是总门厅），这是站长的明确要求——日常最常用的还是邮票馆，总门厅可以从邮票馆顶栏"⌂ 总馆"链接进入。
- 桌面有一个"纸上山河.lnk"快捷方式指向这个 bat。
- **这个 bat 修过两个坑**，以后如果又出现"双击窗口一闪就消失"的问题，先查这两处：
  1. `cmd.exe` 双击运行时走的是**系统原生 PATH**，不是 Git Bash 或 VS Code 终端里那个增强过的 PATH，如果 node/npm 没在系统级 PATH 里，会直接报错退出。当前脚本里写死了 `set "PATH=G:\Program Files\nodejs;%PATH%"` 兜底。
  2. 批处理文件的换行符**必须是 CRLF**（`\r\n`），如果被工具编辑成了 Unix 的纯 `\n`，`cmd.exe` 解析 `if (...)` 这种带括号的多行块经常直接解析失败退出——比逻辑报错还早。

---

## 6. 给后续步骤（②③④）接手的人的建议

- **画册馆、名画馆做的时候**，参考本文档第 4.2 节的"整屏专属背景"模式（`body:has(.xxx-root)`），保持三馆"各有各的底色和身份，但技术实现是同一套模式"。
- **馆的代表色已经定了**（4.3 节），画册用靛蓝、名画用金，各馆内部设计延用即可，不用重新纠结配色。
- 遇到"这个视觉方案站长会不会满意"拿不准的时候，参考第 4.2/4.3 节列的"已经被否掉的方案"，避免重复踩坑（比如别再提议"和邮票馆同款圆角卡片"或"纯文字无边框题签"）。
- 数据/标签相关的规则仍然全部在 `数据录入手册.md`，这份文档**只管前端架构和视觉**，两者不重复。

---

## 7. 画册馆当前协作入口

画册馆已经不再是占位页。后续增加画册、调整轮播、修改图片路由或阅读交互时，先读：

- `docs/画册馆协作交接.md`：当前状态、文件职责、素材边界、固定交互与验收方法。
- `AGENTS.md`：全项目端口、图片仓库、提交与推送规则。

不要只依据聊天记录或临时 handoff；继续工作前还要核对当前代码、`git status --short` 和实际 3011 页面。
