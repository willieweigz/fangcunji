# 方寸集 · 邮票数据录入手册

> **给谁看**：任何负责"新增邮票数据"的 AI 助手或人工录入者。
> **能做什么**：为网站添加新年份/新套票的数据和图片。
> **不能做什么**：修改网站代码、页面、样式、组件、配置（这些属于框架改动，交给主力开发处理）。见文末"禁区清单"。

---

## ⚠️ 工作流程已反转（2026-07-03 起，务必先读这段）

**旧逻辑（已废弃，不要再用）**：录入某年份数据时，AI 现场给每套票想标签，录完之后再回过头去更新标签库。

**新逻辑（当前生效）**：**标签永远是提前定好的**，录入年份数据时**不用打标签、也不该自己造标签**，只需要照抄。分工是：

1. 站长和主力 AI（Claude Code）**先**批量规划好某个时期全部套票的标签，写进 `data/tag-plan.json`（预打标签表）
2. 录入某年份数据的 AI **后**开工，逐套查 `tag-plan.json`，把标签**原样抄进** `data/stamps/<年份>.json`，自己不判断、不新造标签
3. 如果查到某套票在 `tag-plan.json` 里没有、或状态是 `todo`：**停下来，只挂一个一级主题，二级留空，`needsReview: true`，写进交付清单**，等站长/主力 AI 补打标签后再回填——**绝不允许自己现场发明一个二级标签**

一句话记忆：**录入年份数据 = 查表抄标签，不是编标签**。详见下方"标签 SOP"的 Step 0。

---

## ⚠️ 完工后必须同步到 GitHub（否则线上网站看不到你的工作）

**这条对下面三种任务（预打标签 / 录入年份数据 / 修改标签）全部适用，缺了这一步等于白做。**

网站已经上线（`https://fangcunji.vercel.app`），部署方式是 **GitHub → Vercel 自动构建**：Vercel 只认 GitHub 仓库里的内容，本地文件改得再好，**不推送到 GitHub，线上网站永远是旧的**，不会自动感知你本地做了什么。

所以任何一次任务，**体检通过（`check_data.py` 0 ERROR）之后，必须执行**：

```bash
git status --short   # 先看清单再 add，见下方说明
git add .
git commit -m "说明这次做了什么，如：预打2000-2009年标签 / 录入2024年数据"
git push
```

**`git add .` 之前先看一眼 `git status`**：这个项目文件夹可能有多个 AI/会话在用，`git status` 会把自上次提交以来磁盘上的**所有**改动都列出来（不止你这次做的）。这是正常现象、不用紧张，但要扫一眼有没有不该出现的东西（体积异常大的文件、明显不属于这个项目的缓存文件夹等）——历史上出过 24GB 图库和第三方工具缓存文件夹差点被一起提交的事故。正常的项目文件（哪怕是别的会话录入的年份数据）可以放心一起提交，git 允许一次提交包含多来源的改动。详细说明见《部署上线指南.md》。

push 之后 Vercel 会自动重新构建，1–2 分钟后线上网站同步更新，不需要登录 Vercel 做任何操作。

**如果你不确定要不要 push**（比如站长说"先不 push，攒着"）：那就不 push，把改动留在本地工作区，等站长明确说"push 吧"再执行——但这属于站长临时的例外指示，不是常态。**默认规则是：完工 = 体检通过 + push，两者缺一不可。**

git 操作细节（首次配置、遇到冲突怎么办等）见《部署上线指南.md》第二部分。

---

## 0. 先判断：你的任务是"预打标签"还是"录入年份数据"？

这是两个完全不同的任务，分开说明。

### 如果你的任务是"预打标签"（比如站长说"预打 XX 年代/XX 系列的标签"）

这就是本手册"标签 SOP"里 Step 1–4（读现有数据 → 提取关键词 → 匹配已有标签 → 新建须过五规则）真正派上用场的地方。流程：

1. **先核对本地图库年份目录，防止漏套**：如果任务按年份分配，先打开项目根目录下的 `新中国邮票图片全集（1949年-2026年最新）/<年份目录>`，例如 `15-1986年`、`15-1991年`、`52-编年号2024年`，逐个列出里面的**套票子文件夹**，以这个清单作为本年预打标签的底稿。必须记下总套数、每套志号和票名；普通邮票、贺年票、无编号小型张等特殊品类也算一套，不能因为不是 J/T/编年号就漏掉。
   - **遇到命名不一致时，以内部图片文件名为准**：如果子文件夹名、文件夹内 `.md` 标题、`tag-plan.json` 的 `title` 与内部图片文件名不一致，优先相信内部图片文件名里的志号、票名和组别；先按内部图片名修正文件夹名/预打表标题，再继续打标，并在交付说明中注明。
2. **把图库底稿与 `data/tag-plan.json` 对照**：逐套确认预打表里有对应 `id`。如果图库里有、预打表里没有，或预打表数量与图库年份目录数量对不上，先停下来核查原因，并在交付说明里列出；不要只处理 `tag-plan.json` 里已经有的条目，更不能让图库里真实存在的套票静默漏打。
3. 打开 `data/tag-plan.json`，找到分配给你的范围内、状态是 `todo` 或需要复核的 `auto` 条目
4. 按标签 SOP 的 Step 1–4 逐套决定 `themes`（一级唯一在首位、二级 1–3 个、从泛到专排序、系列票对齐已有组、生肖/建交/人物等专项规则都适用）
5. **直接在 `data/tag-plan.json` 里改这些条目**：把 `themes` 改成你定好的数组，**把 `status` 改成 `"manual"`**（这一步必须做，否则下次刷新预打表时你的结果会被冲掉，见下方原理说明）
6. **不要运行 `build_tag_plan.py` 来"生成"标签**——这个脚本只做两件事：从图库同步发现新志号（写成 `todo`）、按几条简单规则猜一部分（写成 `auto`），它不会、也不该替你做真正的标签判断。标签决策必须是你直接手写进 JSON 的
7. 完工后运行 `python scripts/check_data.py`（会顺带校验 `manual` 状态的预打条目是否合规，0 ERROR 才算完工），并查看《标签总表.md》里的"预打标签表"章节确认格式正确
8. **`git add . && git commit && git push`**（见上方"完工后必须同步到 GitHub"，不 push 等于白做）
9. 交付说明：本批次覆盖的范围（如"2000–2009 年共 266 套"）、本地图库核对到的套数、预打表缺失/数量不一致清单、新建的二级标签清单，供站长过目

**为什么要设成 `manual`**：`tag-plan.json` 里 `confirmed`（已录入真实数据的，每次都用真实标签覆盖）和 `manual`（人工定好的，重新生成时原样保留）之外，`auto`/`todo` 每次重跑 `build_tag_plan.py` 都会被**重新计算覆盖**。只有标记成 `manual`，你的预打结果才不会在下次刷新时消失。

### 如果你的任务是"录入某年份数据"

**第一件事：做第 2 节开头的"🚦 开工前置检查"**（确认标签就位、图片已分文件夹、md 文档齐全），三项都满足才动手。然后见"工作流程已反转"和第 2 节——查 `tag-plan.json` 照抄，不自己决定标签。

### 如果你的任务是"修改/新增某套已经录入过的标签"（该套票已经在 `data/stamps/<年份>.json` 里）

**直接编辑那个年份的 JSON 文件本身**，改它的 `themes` 数组。这是唯一真数据源。

⚠️ **不要去改 `tag-plan.json` 里对应的条目**（就算它状态是 `confirmed` 也不要碰）——`confirmed` 状态的条目只是从 `data/stamps/*.json` 同步过来的**只读镜像**，每次运行 `build_tag_plan.py` 都会用真实数据重新覆盖它。改镜像不会生效，等于白改，真正的数据永远只在年份 JSON 文件里。

改完之后：
1. 跑 `python scripts/check_data.py`（会自动刷新标签总表）
2. 如果这套票同时也是某个"系列票"的一员（比如你把《XX（三）》的标签改了），记得检查同系列的其他组（一）（二）是否也要跟着改，保持系列对齐
3. **`git add . && git commit && git push`**（见上方"完工后必须同步到 GitHub"）
4. 交付说明里注明改了哪些套、为什么改

---

## 1. 项目速览

这是一个静态邮票图鉴网站（Next.js），**所有邮票内容都来自数据文件，添加邮票不需要写任何代码**。

```
邮票网站/
 ├── data/stamps/           ← 邮票数据，按年份一个 JSON 文件（如 2024.json）★你的工作区
 ├── public/images/stamps/  ← 邮票图片，按年份一个子文件夹（如 2024/）    ★你的工作区
 ├── scripts/               ← 辅助脚本（图片导入、白边裁剪）
 └── app/ components/ lib/  ← 网站代码 ⛔ 禁止改动
```

数据文件保存后，本地开发服务器（`npm run dev`，访问 http://localhost:3000）会自动读取并生成页面，无需其他操作。

## 2. 添加一个年份 = 五步流水线

### 🚦 开工前置检查（三项全部满足才能开始，缺任何一项立即停下报告站长）

**最快的办法：直接看 `录入进度表.md`**（由 `python scripts/build_progress.py` 自动生成，体检时也会刷新）。这张表按年份列出了①标签 ②图库整理 ③md文档 三项前置条件的状态，最后一列"可开工?"直接告诉你能不能开始。只有该年份三项都是 ✅ 才能动手；有 ⚠️ 或 ❌ 的，说明还缺东西，停下报告站长。

下面是三项条件的详细含义（进度表判断依据），**任何一项不满足都不要开始录入，直接告诉站长"XX 年缺少 YY，请先补齐"**——硬着头皮做只会产出残缺数据，返工更麻烦。

**① 标签已就位**：打开 `data/tag-plan.json`，确认该年份的套票**都有对应条目且状态是 `manual` 或 `confirmed`**（不是 `todo`、也不是查不到）。标签是站长/主力 AI 预先规划的，录入者只照抄不新建（见"标签 SOP"Step 0）。若发现有 `todo` 或缺条目 → 停下，请站长先预打标签。

**② 图片目录已整理成"每套一个文件夹"**：在图库 `新中国邮票图片全集（1949年-2026年最新）/`（就在项目根目录下）里找到该年份的时期文件夹（如 `15-编年号1992年`），确认里面**每套票是一个独立子文件夹**，文件夹名以"志号 《票名》"开头（如 `1992-1 《壬申年》`），且图片文件名已按邮票编号+名称规范命名（如 `1992-1-壬申年-猴2-1猴桃瑞寿.JPG`）。若图库还是一堆散图没分文件夹、或命名混乱 → 停下，请站长先整理图库。

> **命名冲突处理**：如果文件夹名、`.md` 标题、预打表标题和内部图片文件名不一致，**以内部图片文件名为准**。先按内部图片名更正文件夹名；预打标签时同步修正 `data/tag-plan.json` 的 `title`，录入数据时也以内部图片名对应的志号/票名为准。

**③ 每套都有介绍 md 文档**：确认每个套票文件夹里都有一个 `.md` 文件（如 `1992-1 《壬申年》.md`），内含发行公告信息（图名、面值、发行量）+ 背景介绍。这是 `description`、`denomination`、`quantity` 三个字段的主要数据来源。若某些套缺 md → 停下报告站长缺哪几套（缺 md 的套可先跳过或标 `needsReview: true`，但要在交付说明中列清）。

> **达标样板**：`15-编年号1992年` 文件夹——20 套每套一个子文件夹、图片规范命名、md 文档齐全且格式统一（发行公告表 + 发行量 + 背景介绍）。遇到某年份不确定"算不算整理好了"，对照这个样板判断。

---

> 前提：上述三项前置检查全部通过；录入顺序跟着标签预打进度走，站长会指定录哪个时期。

1. **官网落实 + 图库 md 查背景**：逐套查中国邮政官网发行公告，一次落实全部字段——名称、志号、发行日期、**枚数、有无小型张/小全张**、每枚图名和面值、设计者、发行量、内容介绍。**同时打开图库中该套票文件夹内的 `.md` 文件**（路径见第 5 节图库位置，几乎每套票文件夹里都有，⚠️ **容易被跳过，必须打开**）——里面既有"背景资料"段落（画作创作年代与构图、设计理念、印制工艺、人物流派、版式亮点等，写进 description 用），**也有"计划发行数量"一节（填进 `quantity` 字段用，见第 3 节字段说明）**，两处都要提取，不要只看一半。官网公告 + 图库 md 两来源合一，一次查全，`needsReview` 才能是 `false`
2. **写数据文件**：按第 3 节格式创建 `data/stamps/<年份>.json`。标签从预打表**照抄**（见标签 SOP 的 Step 0）；**description 按"介绍撰写规范"写**（见第 3 节字段说明表下方）；介绍中的**地名必须用 `**地名**` 包裹**（网站会以黄底红字突出显示，这是硬性要求不是建议），如 `展现**安徽巢湖**风光`、`原作现藏**故宫博物院**`、`在**浙江温州**举行`
3. **导入图片 + double check**：按第 5 节运行导入脚本。脚本会自动报告"图库文件数少于枚数"的套票（本地图库不一定齐全），缺图**不阻塞录入**（网站显示占位图），但必须写进交付说明
4. **体检**：`python scripts/check_data.py --images` 做到 0 ERROR
5. **交付**：刷新标签总表，提交交付说明（数据来源链接、缺图清单、needsReview 清单、新建标签清单）
6. **同步到 GitHub**：`git add . && git commit -m "录入 XX 年数据" && git push`（见"完工后必须同步到 GitHub"一节，不 push 线上网站看不到新数据）

## 3. JSON 数据格式

文件是一个**数组**，每个元素是一**套**邮票，按志号顺序排列。完整示例：

```json
[
  {
    "id": "2024-1",
    "series": "编年",
    "type": "特种邮票",
    "title": "甲辰年",
    "issueDate": "2024-01-05",
    "year": 2024,
    "themes": ["生肖", "龙"],
    "designer": "王虎鸣",
    "totalStamps": 2,
    "extras": ["小型张"],
    "description": "第四轮生肖邮票龙年票，胶雕套印，北京邮票厂印制。",
    "needsReview": false,
    "localImageFolder": "2024-1 《甲辰年》",
    "stamps": [
      { "sn": 1, "name": "天龙行健", "denomination": "1.20元", "image": "/images/stamps/2024/2024-1-1.jpg" },
      { "sn": 2, "name": "辰龙献瑞", "denomination": "1.20元", "image": "/images/stamps/2024/2024-1-2.jpg" }
    ]
  }
]
```

### 字段说明

| 字段                      | 必填                 | 规则                                                                                                                                                                                                                                               |
| ----------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `id`                    | ✅                  | 志号，全站唯一。编年票如 `2024-1`；历史票用官方志号如 `纪1`、`特57`、`文7`、`编14`、`T46`、`J8`；贺年专用邮票如 `贺1`                                                                                                                                                                    |
| `series`                | ✅                  | 只能是：`纪` / `特` / `文` / `编` / `J` / `T` / `编年` / `贺`（贺年专用邮票）/ `普`（普通邮票）。**没有"JT"这个值**——1974–1991 年的志号本身就分 J（纪念邮票）和 T（特种邮票）两类，和 1967 年前"纪/特"是同一逻辑，`series` 按志号首字母填 `J` 或 `T`，不要合并。其他特殊品类（包裹邮票、加字改值邮票、欠资邮票等，见"标签 SOP"末尾"特殊邮票品类"一节）遇到时按各自志号系统填，暂无统一枚举 |
| `type`                  | ✅                  | `纪念邮票` 或 `特种邮票`；普通邮票填 `普通邮票`（不属于纪念/特种）                                                                                                                                                                                                           |
| `title`                 | ✅                  | 套票名称，不带书名号                                                                                                                                                                                                                                       |
| `issueDate`             | ✅                  | `YYYY-MM-DD` 格式                                                                                                                                                                                                                                  |
| `year`                  | ✅                  | 数字，与 issueDate 年份一致                                                                                                                                                                                                                              |
| `themes`                | ✅                  | **`themes[0]` 必须是一级主题**（存在于 `data/themes.json`），且全数组有且只有这一个一级；后面是 0–3 个二级标签。打标签必须走下方"标签 SOP"                                                                                                                                                     |
| `designer`              | 可空                 | 设计者，未知填 `""`                                                                                                                                                                                                                                     |
| `totalStamps`           | ✅                  | 全套正票枚数（不含小型张等附加条目），必须等于 `stamps` 数组中不带 `format` 字段的条目数                                                                                                                                                                                           |
| `extras`                | ✅                  | 附属品数组，**只填"小型张"和"小全张"**（含其变体，如"三地联合小全张"统一写"小全张"），其他版式（小版张、小本票、版式二、长卷版等）一律不填。没有填 `[]`                                                                                                                                                             |
| `description`           | ✅                  | 邮票背景介绍。**不是一句话应付**，需按下方"介绍撰写规范"撰写，整合图库 md 背景资料。**地名必须用 `**地名**` 包裹**（硬性要求，页面上会以黄底红字突出），其他内容不要用此标记。如 `原作现藏**故宫博物院**`                                                                                                                              |
| `quantity`              | ✅（图库有 md 时必填，其余可空） | 发行量，**只写整套（套票）的发行量即可**，如 `1237.7万套`；**不必**再列小本票、版式二、小型张等各变体的分项数字。来源是图库该套票文件夹内的 `.md` 文件，**格式因年代而异，两种都取"套票总发行量"那一个数**：① 较新的 md（如 2024 年）有独立的"计划发行数量"一节，取里面套票那一行；② 较早的 md（如 1992 年）把发行量分散写在每枚后面、末尾另有合计——取整套的合计数。这个 md 文件几乎每个文件夹都有，**容易被忽略，务必打开看**。md 没有、官网也未公布，才省略此字段                                                                    |
| `source`                | 可空                 | 数据来源 URL（官网公告链接），不在页面显示，供日后核对                                                                                                                                                                                                                    |
| `needsReview`           | ✅                  | 任何字段没把握就填 `true`（见第 4 节）                                                                                                                                                                                                                         |
| `localImageFolder`      | 可空                 | 本地图库中该套的文件夹名，方便日后对照                                                                                                                                                                                                                              |
| `stamps[].sn`           | ✅                  | 枚序，从 1 开始                                                                                                                                                                                                                                        |
| `stamps[].name`         | ✅                  | 单枚图名；**查不到就填 `（图名待核对）`，严禁编造**                                                                                                                                                                                                                    |
| `stamps[].denomination` | ✅                  | 面值如 `1.20元`、`80分`。**必须尽量查到并填写**——面值是邮票的基本身份信息，会显示在网站上（每枚图片下方 + 详情页），不是可有可无的装饰字段。只有确实查不到官方数据时才留空 `""`，并把该套 `needsReview` 设为 `true`                                                                                                                |
| `stamps[].image`        | ✅                  | 固定格式：`/images/stamps/<年份>/<志号>-<枚序>.jpg`（即使图片还没复制也这样填，页面会自动显示占位图）                                                                                                                                                                                |
| `stamps[].format`       | 可选                 | 附加条目类型，如 `小型张`。**小型张可以作为 stamps 数组末尾的附加条目录入**（sn 顺延），必须带此字段；正票条目不填。因此 stamps 数组长度 = totalStamps + 附加条目数。纯小型张发行的套票 totalStamps 填 0                                                                                                                |

### 介绍撰写规范（description 字段）

description 不是"志号+枚数+面值"的流水账——那只是发行公告里就有的基本信息，读者看图就知道了。**description 的职责是讲邮票背后的故事**，让读者在浏览邮票时能看到背后的信息。本站的一个定位是"看邮票的同时长见闻"，所以信息丰富的题材**鼓励适当展开**，把票面背后值得一说的知识讲给读者——但展开要有度，度的把握见下方"展开的度"。

**信息来源**：图库中每套票文件夹内的 `.md` 文件（路径见第 5 节图库位置）。该 md 通常包含两类内容：
- **发行公告信息**（志号、面值、规格、齿孔、版别等）—— 这些**不写进 description**，它们是数据字段，不是介绍
- **背景资料**（画作渊源、设计理念、印制工艺特色、人物流派、版式亮点等）—— **这才是 description 要提炼的内容**

> ⚠️ **md 背景段落的长度因年代而异，长的必须删减、不要照抄**：较新的 md（如 2024 年起）背景资料往往写得很长很细，知识密集题材（如稻城亚丁）可能近千字；较早的 md（如 1992 年）通常只有一两百字。**md 长 ≠ description 就写这么长**——不论源 md 多长，description 都要按下方"展开的度"的分层字数上限，用"锚点+一层"把最值得讲的那部分**提炼、适当删减**出来，绝不整段搬运（整段搬 = 百科搬运，正是要避免的）。反过来，md 短或只有发行公告信息时，按本节末尾"如果 md 文件没有背景资料段落"的兜底规则自己补两三句。

**该写什么**（按邮票类型灵活取舍，有几条写几条）：

| 信息维度 | 说明 | 示例 |
|---|---|---|
| 画作/文物背景 | 古画题材：作者、年代、材质、馆藏地、构图特点 | `取材元代画家任仁发《出圉图》，绢本设色画，作于至元庚辰（1280年），现藏**故宫博物院**` |
| 设计理念 | 邮票设计想要表达什么 | `以和谐共生为理念，表现绿树、山茶花、瓯柑等植物元素` |
| 印制工艺 | 有特殊工艺时必须提及 | `采用胶雕套印工艺`、`茶花香味油墨与三维起凸技术`、`六色胶版+三维起凸，印文以高亮红墨呈现朱膘印泥韵味` |
| 人物/流派 | 人物题材邮票：身份、流派、成就 | `**丁敬**——浙派开山鼻祖、西泠八家之首，独创切刀法` |
| 版式亮点 | 有互动/特殊设计时提及 | `版式二揭开后背面有纸飞机折叠引导线，将邮票从收藏延伸至互动` |
| 活动背景 | 纪念活动/展会：时间、地点、主题 | `该届园博会于2026年4月至7月在**浙江温州**举行，主题为"诗画山海·共享绿色生活"` |

**不该写什么**：志号、面值、齿孔度数、整张枚数、整张规格、防伪方式、印制厂、发行量 —— 这些要么已在其他字段里，要么属于枯燥的规格参数，读者不需要在介绍里重复看到。

**展开的度（重要）**：允许"涨见闻"式的展开，但**不是靠字数放开，而是靠两条闸门约束**，否则不同人尺度不一、还容易滑成"百科搬运"（把资料整段抄贴、脱离邮票本身）。

- **闸门一 · 锚点**：展开的内容必须**锚定在票面上真实出现的人 / 物 / 事 / 画 / 建筑上**，是"这张票画的东西"值得多说的一层，不能脱离邮票去讲无关的大背景。
  - ✅ 曾侯乙编钟票 → "这套编钟共 65 件、总重 2500 余公斤，能演奏完整的十二音律，是目前出土规模最大的一套先秦青铜编钟"（锚在票面文物上）
  - ❌ 曾侯乙编钟票 → 大段铺陈整个先秦音乐制度史（脱锚，变成论文）
- **闸门二 · 一层**：在"这是什么"的基础上，**只再深入一层**给一个具体的知识点 / 冷知识 / 历史意义就收住，不层层铺开、不追加第二第三层。这条是"越写越收不住"的刹车。

**长度参考（按题材分层，替代旧的死字数）**——字数只是参考区间，真正的取舍看上面两条闸门：

| 题材类型 | 参考区间 | 说明 |
|---|---|---|
| 常规题材（建筑、风光、活动 / 会议纪念、体育赛事等） | 90–150 字 | 交代背景即可，一般不强行展开 |
| 知识密集题材（古画、文物、历史人物、科技、历史事件、文学名著、生肖民俗等） | 150–280 字 | "涨见闻"主战场，放 1–2 个锚点，每个锚点只深一层 |
| 单枚小票（题材普通） | 80+ 字可 | 但若题材属上一行"知识密集"，仍按知识密集展开 |
| 多人物 / 系列合集（如"挂像英模""历史人物"，每枚对应一位不同的人） | 每人一句，不设总上限 | 每个人都要有一句简介（人名标记+生卒年+一句代表性成就），不能只写"共X位杰出代表"这种笼统概括 |

> 📌 **后续规划（暂不实施）**：等全目录数据录入接近完成后，会一次性把"延伸见闻"从 description 拆成独立字段（如 `extendedNote`）并在详情页做"拓展阅读"版块（即方案 B）。**现阶段先按方案 A**：见闻内容就写在 description 里，靠上面的分层+双闸门控制。所以现在把见闻写进 description 是对的，将来迁移到独立字段时这些内容正好是现成素材，不会白写。

**加粗规则**：**地名/机构名**和**人名**（历史人物、票面题材相关人物）用 `**xxx**` 包裹，都会以黄底红字突出显示（如 `**故宫博物院**`、`**浙江温州**`、`**丁敬**"敬身六面印"`）。**唯独设计者/雕刻者/责任编辑的名字不加粗**——这是制作方信息，不是票面内容。工艺名、系列名等其他内容不加粗。

**人名生卒年规则**（2026-07-04 新增）：**人名（不分古代/现代、不分中国/外国）第一次出现时，加粗名字后紧跟括号写生卒年**，格式 `**姓名**（出生年-死亡年）`，如 `**陈毅**（1901-1972）`、`**苏轼**（1037-1101）`。生卒年**必须查证准确、禁止编造或估算**——查不到确切年份的，宁可不写括号，也不能瞎填；如有把握不足，该套 `needsReview` 设为 `true`。仅在世人物或生卒年确实无法查证时可省略括号。

**好的 description 示例**：

> 中国篆刻系列第三组，全套4枚，选取清代四位篆刻大家代表作：**丁敬**"敬身六面印"，浙派开山鼻祖、西泠八家之首，独创切刀法；**邓石如**"江流有声断岸千尺"，皖派集大成者，开创"以书入印"范式；**赵之谦**"二金蝶堂"，晚清革新大师，力主"印外求印"；**黄士陵**"十六金符斋"，黟山派创始人，追求光洁妍美。采用六色胶版+三维起凸工艺，印文以高亮红墨呈现朱膘印泥韵味。

**差的 description 示例**（只有基本信息的流水账）：

> 中国篆刻系列第三组，全套4枚，表现丁敬、邓石如、赵之谦、黄士陵等清代以来篆刻名家代表作品。

**如果 md 文件没有背景资料段落**（只有发行公告信息）：自己写 2–3 句概述，至少交代邮票主题的背景（如"表现AI技术从诞生到应用的演进"），不要只写"全套X枚"。

### 标签 SOP（每套邮票打标签的标准流程）

标签分两级：**一级主题**是邮票的唯一"户口"，清单维护在 `data/themes.json`（就是"按主题浏览"页显示的大类）；**二级标签**是细分（系列名、人名、地名、赛事等），自动出现在一级主题页的"细分浏览"按钮里，**不要**加进 `themes.json`。

**Step 0 — 先查预打标签表（最优先）**

全目录 1500+ 套的标签在 `data/tag-plan.json` 中统一规划（站长审定）。录入某套票时先按志号查这张表：

| 表中状态 | 怎么做 |
|---|---|
| `confirmed` / `manual` | **原样照抄** themes，一个字都不要改 |
| `auto` | 一级主题和已有标签**照抄不动**；唯一允许做的"补全"是给已确定的分类**填具体名字**（比如"建交"类已经定好，只是缺国名，从公告里抄国名填上；生肖贺年票缺动物字，按生肖对照表填上）——**不是自己判断该归哪一类，分类本身已经定好了**，交付说明中注明补了什么 |
| `todo` 或表里没有 | 按 SOP 挂一个**一级主题**（二级留空），该套 `needsReview` 设为 `true`，并在交付说明的"待确认标签清单"中列出，等站长确认——**不阻塞整年录入，但绝不自创二级标签** |

Step 1–4（自建标签的完整流程）**只有站长/主力 AI 在做预打表规划时才会用到**。录入年份数据的 AI 正常情况下走不到这里——如果你发现自己在按 Step 1–4 的方法"思考这套票该挂什么标签"，先停下来，大概率说明你应该用上面的 Step 0 而不是自己判断。

**Step 1 — 读现有数据**
- 加载 `data/themes.json`（一级主题清单，记在脑子里）
- 搜集 `data/stamps/*.json` 里已经用过的所有二级标签

**Step 2 — 提取关键识别词**（从邮票标题中提取）
- 系列名（去序号）：「中国名亭（二）」→「中国名亭」
- 人名：「张仲景」→「张仲景」
- 地名：「洞庭湖」→「洞庭湖」
- 事件名：「二十大」→「二十大」
- 作品名：「货郎图」→「货郎图」

**Step 3 — 匹配已有标签**
拿 Step 2 的关键词去 Step 1 的已有标签里对：命中了 → 直接用已有的，不改词；没命中 → 走 Step 4 新建。

**系列票必须做对齐检查**：录《XX（三）》之前，先搜数据里的《XX（一）》《XX（二）》用的是什么系列标签，**一字不差地照抄**——这是系列能跨年份聚合的前提。注意两个坑：① 系列名必须写全（`中国剪纸` 不能简写成 `剪纸`、`中国古镇` 不能简写成 `古镇`），因为可能存在同名近亲系列（如 50 年代《中国古代科学家》与 2018 起的《中国古代科学家及著作》是两个系列）；② 大系列套小系列时用最具体的系列名（《中国古代书法——篆书（二）》→ `篆书`，可同挂 `中国古代书法`；《中国古典文学名著——〈红楼梦〉（四）》→ `红楼梦`）。

**Step 4 — 新建标签须通过五规则**

| 检查项 | 通过标准 |
|---|---|
| 一级唯一 | `themes[0]` 在一级列表中，`themes[1:]` 没有任何一级词 |
| 系列完整 | 系列名写全，只去序号（「封神演义（一）」→「封神演义」） |
| 用词统一 | 不存在同义异名（不允许「奥运会」和「奥林匹克」共存） |
| 不拆复合词 | 「世界自然遗产」就是一个完整标签 |
| 关键词不缺 | 标题里的核心词都能在标签里找到 |

**补充规则：**
- **排序从泛到专**：聚合力强的词在前，专属词在后。如 `["历史事件", "建交", "墨西哥"]`、`["植物花卉", "花卉", "芍药"]`、`["历史事件", "澳门", "澳门回归"]`
- **数量 1–3 个，强制要求，用"第二套测试"取舍**：**每套邮票必须至少有 1 个二级标签**，`check_data.py` 会强制校验（0 个或超过 3 个都是 ERROR）。选哪个词：这个词将来会不会有第二套票挂它？会 → 优先；实在找不到再退而求其次，挂一个宽泛但准确的词（如 `民生`、`国际组织`），也比不挂强
- **套票标题不默认做成标签**：《秦岭》不需要"秦岭"标签；只有预期跨套聚合的名字才挂（如 `莫高窟`、`故宫博物院` 有多组发行）。历史数据中已存在的同名标签可保留，不必清理
- 二级标签**最多 3 个**，宁缺毋滥（在"至少 1 个"的前提下）

**"周年纪念"一级主题（2026-07-03 从历史事件拆分而来）**：凡是"纪念某机构/关系/地区成立满 X 周年"这种结构的套票，一律归 `周年纪念`，不归 `历史事件`。包括：
- 建交纪念（二级：`建交` + 国名）
- 大学建校（二级只用 `大学建校`，不再加校名）；军校/党校等非大学机构建校也归此类（二级用具体机构名，如 `黄埔军校`、`中共中央党校`）
- 机构/组织成立周年（红十字会、总工会、共青团、政协、全国人大、万国邮联、联合国及其下属机构等，二级用机构名）
- 自治区成立周年（二级：`自治区成立` + 地区名）

而长征抗战胜利改革开放五四运动国庆香港回归澳门回归这类**本身就是重大历史事件**（不是单纯的机构成立），仍归 `历史事件`。地区相关的周年纪念，如西藏和平解放70周年，可归 `周年纪念`，二级用地区名（如 `西藏`）。判断标准：去掉周年二字后，剩下的是不是一个独立机构/关系/地区的名字——是则归周年纪念，不是（是一场运动/战争/政治事件本身）则归历史事件。

**历史事件内部的常用聚合二级标签**（新增套票优先复用）：
- `区域发展`：京津冀、长江经济带、大湾区、长三角、雄安新区、海南自贸港等区域战略
- `国际峰会`：G20、APEC、金砖国家、上合组织等国际会议/峰会
- `人大` / `党史`：沿用即可

**"节日"一级主题（原"民俗节日"改名）**：二级分两类——
- `民俗节日`：中国传统节日。除 `拜年` 因有系列聚合价值可作为第三个标签外，其他传统节日一般停在 `民俗节日`，不再细分到中秋、清明、元宵等小标签
- `现代节日`：现代设立的主题日/纪念日/西式节日（护士节、教师节、警察节、记者节、植树节、禁毒日、老年人日、助残日、丰收节、烈士纪念日、感恩父亲/感恩母亲、各类“国际/全国 XX 日”等）。标签停在 `现代节日`，不再细分小节日名。**注意与“周年纪念”的区别**：这类是每年都过的常规节日/主题日，不是“设立满 X 周年”的一次性纪念

**"人物"一级主题**：二级第一位固定用 `古代人`（1840 年前）或 `现代人`（1840 年后，含近现代），**不再使用具体人名作为标签**（华佗、屈原、张骞等单人纪念票录入时不挂人名，只挂古代人/现代人 + 可选的专长标签，如中医药、佛教、丝绸之路）。系列类人物票（中国现代科学家、中国古代文学家等）系列名照常保留，同时追加古代人/现代人。**外国人物**（马克思、恩格斯、外国音乐家等）不参与古代人/现代人二分，只挂主题词（如 `马克思主义`）或系列名。**近代爱国将领/民主人士**（杨虎城、张学良、各民主党派人士等）归 `人物` 一级，二级用 `现代人` + `爱国民主人士`。

**"共产党人"一级主题下的党和国家领导人**，二级按级别区分（依据：领袖级人物的纪念邮票在设计规格上就与其他革命家不同，通常是特殊横票规格）：
- `领袖`：**毛泽东、周恩来、邓小平、刘少奇、朱德** 这五位（毛、邓因题材多另加独立人名标签，如 `["共产党人","毛泽东","领袖"]`）
- `老一辈革命家`：其他革命家和早期领导人（宋庆龄、李立三、方志敏、习仲勋、韦国清、宋任穷等）
- 军事将领归 `解放军` 一级（不是共产党人）：元帅用 `十大元帅`（聂荣臻、刘伯承、罗荣桓等），非元帅的军事家用 `军事家`（叶挺等）
- 新建的二级标签必须在交付说明中**单独列出**，供站长人工过目
- 新增**一级主题**时必须同步把名字加进 `data/themes.json`（否则该套票不会出现在按主题页）
- **生肖票**：一级 = `生肖`，二级 = `生肖鼠` `生肖牛` `生肖虎` `生肖兔` `生肖龙` `生肖蛇` `生肖马` `生肖羊` `生肖猴` `生肖鸡` `生肖狗` `生肖猪`。不要用动物单字，避免和文物、绘画等题材里的 `虎`、`马` 混在同一个标签页；**禁止用干支年名**（庚子年、甲辰年等）做标签——60 年才轮回一次，没有聚合价值
- 建交纪念票：二级必须带 `建交`
- **植物花卉票**：二级第一个用种类词（`花卉` / `树木` / `菌类` / `药用植物`），具体品种名（如 `芍药`、`玫瑰`）作第二个二级标签，例：`["植物花卉", "花卉", "芍药"]`
- **贺年专用邮票**（志号"贺X"，图库文件夹"**33-贺年专用邮票2006年-**"，图片文件名自带志号和票名，无需看图识别；注意"41-贺卡专用邮票2013年-"是另一个系列，勿混淆）：一级 = `贺年专用邮票`，`series` 填 `贺`，仍按发行年份放进对应的 `<年份>.json`；生肖题材的贺年票二级可挂动物单字

**普通邮票（普票）与其他特殊邮票品类**：

普票、包裹邮票、军用邮票等属于"邮票品类"而非"内容主题"——它们和纪念/特种邮票是完全不同性质的邮政产品（日常流通、无纪念意义、常见多版别/多次改值），**不按图案内容归类到"文物古迹""建筑""动物"等已有一级主题**，否则会和同图案的纪念性质邮票混在一起（比如普票《长城》不能跟纪念性质的 2016-22《长城》一起挂进"文物古迹"）。处理原则参照上面的"贺年专用邮票"：**一级主题就用品类本身的名字**（同步加进 `data/themes.json`），二级标签按图案/内容细分（这样同图案跨版别的票能聚合，如普28《长城》、普29《万里长城（明）》都挂 `["普票", "长城"]`），`type`/`series` 按该品类实际情况填，数据仍按**发行年份**放进对应的 `<年份>.json`（不单独开品类文件）。

- **普通邮票**（志号"普X"/"普东X"/"普旅X"，图库文件夹"**05-普通邮票1950年-**"，文件名自带具体发行年份前缀，直接对应该放进哪个 `<年份>.json`）：一级 = `普票`，`series` 填 `普`，`type` 填 `普通邮票`
- 以下几类图库里已经整理好但**还没进 `data/themes.json` 也没预打标签**，遇到时套用同一原则（一级 = 品类名，记得同步加进 `data/themes.json`）：
  - `02-包裹邮票`（1950年）、`03-加字改值邮票`（1950-1951年）、`04-欠资邮票`（1950-1954年）、`06-航空邮票`（1951年）、`08-军用邮票`（1953-1997年）
  - `14-最佳邮票评选纪念张`（1981年-）、`23-自动化邮票`（1999年）、`24-特别发行邮票`（2000年-）、`28-个性化服务专用邮票`（2002年-）、`41-贺卡专用邮票`（2013年-，勿与"33-贺年专用邮票"混淆）
- `13-小本票`**不是**独立品类——小本票只是既有套票的装帧形式，按字段说明表里 `extras` 字段的既有规则处理（小本票不单独作为一级主题，也不填进 extras），不要新建一级主题

## 4. 数据质量红线（最重要的一节）

1. **宁可留空，严禁编造**。图名、面值、设计者查不到确切资料时，图名填 `（图名待核对）`、其他填 `""`，并把该套 `needsReview` 设为 `true`。**绝对不允许根据"合理推测"填写内容。**
2. **来源优先级**：中国邮政官网发行公告（chinapost.com.cn）＞ 国家邮政局公告 ＞ 专业邮票目录网站（如 meetstamps.com、52youpiao.com、stampsms.com）＞ 其他。至少两个来源一致才算核实。
3. **交叉校验**：全套面值合计应等于各枚面值之和；`totalStamps` 必须等于 `stamps` 数组中**不带 format 字段**的条目数；发行日期年份必须等于 `year`（`year` 为必填数字，漏填会导致页面出现"NaN 年代"）。
4. `needsReview: false` 只有在"每枚图名、面值都经官方公告核实"后才能设置。

## 5. 图片导入

### 图库位置（本机）

```
新中国邮票图片全集（1949年-2026年最新）/   （就在项目根目录下，与 data/、public/ 同级）
```

> ⚠️ 这个图库约 24GB，**已在 `.gitignore` 里排除，绝不能提交到 git 或部署**。它只是本地录入时的素材来源；网站真正用的图片是筛选压缩后放进 `public/images/stamps/` 的那批。

- 图库按时期分文件夹（如 `52-编年号2024年`），其中每套票一个子文件夹，名称以志号开头（如 `2024-20 《稻城亚丁》`）
- 套内图片按文件名数字排序后，**前 N 张（N=全套枚数）就是第 1 至第 N 枚的单枚图**，之后是整版票/小型张；带 `(1)` 后缀的是重复文件，忽略
- 此规律已在 2024 年全部 27 套上人工验证成立；**其他年份使用后应抽查确认**

**⚠️ 历史时期（1949–1991，纪/特/文/编/J/T）文件夹命名硬性要求**：子文件夹名**必须用真实志号开头**（如 `T159 《辛未年》`、`J184 《徐向前同志诞生九十周年》`、`纪1 《庆祝中国人民政治协商会议第一届全体会议》`），**不允许套用"年份-序号"格式**（如 `1991-1`）——这个格式只属于 1992 年及以后的编年号邮票，1991 年及更早的邮票用官方志号，两套编号体系不通用。

真实志号的判断方法：**看文件夹内图片的文件名**，不要看文件夹名本身——历史遗留的部分文件夹存在"文件夹名用错格式、文件名却是对的"情况（如文件夹叫 `1991-1 《辛未年》`，里面的图片却正确地叫 `T159-辛未年.jpg`）。发现文件夹名和真实志号对不上时，**先把文件夹名（连同里面的 `.md` 文件名）重命名成真实志号**，再开始录入——不要在这种情况下直接照抄错误的"年份-序号"当作 `id`，那样会污染全站的志号体系。

### 自动导入（推荐）

数据文件写好后，运行：

```bash
python scripts/import_year_images.py 2024        # 把 2024 换成目标年份
```

脚本会按上述规律自动复制并重命名图片到 `public/images/stamps/<年份>/`，并打印每套的映射清单。

### 图片体积标准

网站图片必须满足：**长边 ≤ 1600px，体积 ≤ 400KB**（超大图片会拖慢页面加载）。导入脚本会在复制时自动压缩超标图片；如发现存量图片超标，运行：

```bash
python scripts/compress_images.py
```

原始图库永远不动，只压缩网站 `public` 目录里的副本。

### 导入后必须抽查

打开网站抽查 3–5 套（尤其是枚数多的套票），确认"图片与图名对得上、顺序正确"。如果 AI 能直接查看图片，应逐套核对首枚；发现顺序错乱时，手工重命名纠正，并在 JSON 中把该套 `needsReview` 设为 `true` 留待人工复核。

## 6. 完工自检（强制）

**必须运行体检脚本并做到 0 ERROR 才算交付：**

```bash
python scripts/check_data.py --images
```

脚本会自动检查：JSON 合法性、必填字段、志号唯一、year 与日期一致、totalStamps 与正票条目数一致、一级主题唯一且在首位、二级标签 1–3 个、无干支标签、图片缺失与超标等。WARN 不阻塞交付，但要在交付说明中说明。**体检通过后会自动重新生成《标签总表.md》**，不需要单独再跑 `export_tags.py`（JSON 是唯一数据源，标签总表永远是只读产物，不要手改）。

此外人工确认一项：

- [ ] 启动 `npm run dev`，访问 `/years/<年份>`：所有套票显示、无破图
- [ ] 交付说明中列出：数据来源链接、`needsReview: true` 的套票清单及原因、**新建的二级标签清单**

## 7. 非编年号年份（1991年及以前）图库整理指南

> **适用场景**：录入1991年及以前的邮票数据前，需要先按1992年样板整理图库——这类年份的邮票使用 **T字头/J字头** 编号体系（如T159、J185），图片分散存储在 `12-T字头邮票1974年-1991年` 和 `11-J字头邮票1974年-1991年` 两个大文件夹里，而不是像1992年后那样按年份分文件夹。

### 背景说明

1992年开始，中国邮票改为编年号体系（如1992-1、1992-2……），图库结构也相应调整为"每套票一个子文件夹"。但1991年及以前的T/J字头邮票，图片是按**系列**存储的（所有T邮票在一个文件夹，所有J邮票在另一个文件夹），需要手动按年份拆分重组。

### 操作流程（以1991年为例）

**第1步：确定哪些T/J邮票属于目标年份**

需要网上搜索"YYYY年中国邮票发行目录"，按发行时间顺序列出该年发行的所有T和J邮票。推荐信息来源：
- 今日头条"中华人民共和国按日期发行的邮票"系列
- 百度百科"中华人民共和国邮票目录 (YYYY)"
- 专业邮票目录网站（如 meetstamps.com、52youpiao.com）

搜索结果应整理成表格，包含：志号、邮票名称、发行日期、枚数、有无小型张。

**第2步：创建年份文件夹**

在图库根目录下创建文件夹，命名格式：`15-XXXX年`（所有1992年及以前的非编年号年份统一用 `15-` 前缀）。

示例：
- `15-1991年`
- `15-1990年`
- `15-1989年`
- …（以此类推，所有非编年号年份都用 `15-` 前缀，与 `15-编年号1992年` 保持连贯）

**第3步：从源文件夹复制图片**

T字头邮票从 `12-T字头邮票1974年-1991年` 复制，J字头邮票从 `11-J字头邮票1974年-1991年` 复制。

复制时保留原文件名（如 `T159-辛未年.jpeg`、`J175-巴黎公社一百二十周年.jpg`），不要重命名——后续创建子文件夹时会按规范重命名。

**第4步：创建子文件夹结构**

在年份文件夹内，为每套邮票创建一个子文件夹，命名格式：`T/J编号 《票名》`——**直接用该套票真实的 T/J 志号**（如 `T159`、`J175`），不要仿照 1992 年编年号样板发明一个"YYYY-N"序号（1991年及以前的邮票本来就没有编年号，志号就是 Txxx/Jxxx，这才是它的"真身份证号"）。

示例（1991年）：
```
15-1991年/
├── T159 《辛未年》/
├── T156 《都江堰水利工程》/
├── J175 《巴黎公社一百二十周年》/
├── T160 《计划生育》/
└── ...（共22套）
```

**第5步：创建MD介绍文档**

每个子文件夹内创建一个 `.md` 文件，文件名格式：`T/J编号 《票名》.md`（与子文件夹同名，用真实志号）。

内容模板（参照1992年样板，仅标题行换成真实志号）：
```markdown
# T/J编号 《票名》

中国邮政定于YYYY年M月D日发行《票名》X种邮票1套N枚。详情如下：

（N-1）图名1 面值1
（N-2）图名2 面值2
...

邮票规格：XX×XX毫米

齿孔度数：XX度

整张枚数：XX枚

版别：影雕套印/影写版

设计者：XXX

印制厂：北京邮票厂

发行量：XXXX万套

【背景介绍段落】介绍邮票主题的背景知识（50-150字）。
```

**第6步：验证结构**

与1992年样板对比，确认：
- [ ] 每套票一个子文件夹
- [ ] 文件夹命名格式正确（`T/J编号 《票名》`，用真实志号，不是编年号样式的序号）
- [ ] 每个子文件夹内有 `.md` 文件
- [ ] 图片文件已复制到对应子文件夹
- [ ] 图片文件名规范（保留原文件名即可，导入脚本会处理）

### 注意事项

1. **T/J字头邮票的编号规则**：T代表"特种邮票"，J代表"纪念邮票"。编号中的数字（如T159的159）是累计序号，不是年份。
2. **文件夹排序**：文件夹按T/J志号命名后，在文件管理器里天然不是按发行时间顺序排列的（T和J是两套独立编号），这是正常现象，不用手动调整文件夹顺序；真正决定网站展示顺序的是 `data/stamps/<年份>.json` 里各条目的排列顺序，录入数据时按发行日期排好即可。
3. **小型张/小全张**：如果某套票有小型张或小全张，图片文件名通常包含"-小型张-"或"-小全张-"，需要一并复制到子文件夹。
4. **图片格式**：T/J字头邮票的图片格式可能不统一（.jpg、.jpeg、.JPG等），复制时保留原格式，导入脚本会统一处理。

### 完成标志

年份文件夹结构应与1992年样板一致（只是子文件夹用真实T/J志号命名，而非编年号序号）：
- 22套邮票（1991年）= 22个子文件夹
- 每个子文件夹内有1个MD文件 + 若干图片文件
- 图片文件数量 = 枚数 + 小型张/小全张（如有）

---

## 8. ⛔ 禁区清单（属于框架改动，不要碰）

- `app/`、`components/`、`lib/` 下的一切文件（页面、组件、代码）
- `package.json`、`next.config.ts`、`tsconfig.json`、`postcss.config.mjs` 等配置
- `PRD.md`、本手册
- 已有年份的数据文件中**非数据性**的结构改动（如增删字段、改字段名）——发现格式不够用时，向站长反馈，由主力开发统一调整

如果任务超出"添加/修正邮票数据和图片"的范围，停下来说明情况，不要自行发挥。
